services:
  postgres:
    container_name: sprout_postgres
    image: postgres:18
    ports:
      - "51359:5432"
    volumes:
      - postgres:/var/lib/postgresql
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=main

  valkey:
    container_name: sprout_valkey
    image: valkey/valkey:latest
    ports:
      - "55791:6379"
    volumes:
      - valkey-data:/data

  elasticsearch:
    container_name: sprout_elasticsearch
    image: elasticsearch:9.4.3
    ports:
      - "42601:9200"
      - "42602:9300"
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - xpack.security.enrollment.enabled=false
      - bootstrap.memory_lock=true
      # 1 GB is plenty for a development corpus and keeps this startable on a laptop.
      # A 16 GB heap here is silently OOM-killed (exit 137) by Docker Desktop's memory cap.
      # Production sizes its own heap in docker-compose.production.yml.
      - ES_JAVA_OPTS=-Xms1g -Xmx1g
      - ELASTIC_PASSWORD=wgUi6pBdBJq03hpDw2ulNuW
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - esdata:/usr/share/elasticsearch/data

  garage:
    container_name: sprout_garage
    image: dxflrs/garage:v2.3.0
    restart: unless-stopped
    ports:
      - "51199:3900"
      - "21693:3902"
      - "59387:3903"
    volumes:
      - ./.config/garage.toml:/etc/garage.toml
      - garage-data:/var/lib/garage/data
      - garage-meta:/var/lib/garage/meta

volumes:
  postgres:
    driver: local
  valkey-data:
    driver: local
  esdata:
    driver: local
  garage-data:
    driver: local
  garage-meta:
    driver: local
