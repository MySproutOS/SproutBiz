# Compose interpolation only, deployed to /opt/forum/.env (chmod 600).
# The *_TAG values are managed by bin/deploy/*.sh -- do not hand-edit them.
POSTGRES_PASSWORD=REPLACE_ME
VALKEY_PASSWORD=REPLACE_ME
ELASTIC_PASSWORD=REPLACE_ME
WEBSITE_TAG=latest
BULLGROUND_TAG=latest
MIGRATOR_TAG=latest
